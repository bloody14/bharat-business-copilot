import { ArrowRight, Sparkles } from "lucide-react";
import { MetricCard } from "@/components/dashboard/metric-card";

const metrics = [["Total sales", "₹0", "Ready for live data"], ["Outstanding", "₹0", "No invoices yet"], ["Products", "0", "Build your catalogue"], ["Low stock", "0", "Everything looks good"]];

export default function DashboardPage() {
  return <main className="mx-auto max-w-7xl p-5 lg:p-8"><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p className="text-sm font-medium text-emerald-400">Thursday, 31 July</p><h1 className="mt-1 text-3xl font-semibold tracking-tight">Good morning, Akshay.</h1><p className="mt-2 text-sm text-muted-foreground">Here’s a calm start to your business day.</p></div><button className="inline-flex items-center justify-center gap-2 rounded-lg bg-primary px-4 py-2.5 text-sm font-medium text-primary-foreground"><Sparkles className="size-4" />Ask Copilot</button></div><section className="mt-8 grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{metrics.map(([label, value, detail]) => <MetricCard key={label} label={label} value={value} detail={detail} />)}</section><section className="mt-6 grid gap-6 lg:grid-cols-[1.5fr_1fr]"><article className="rounded-xl border border-white/5 bg-card p-6"><div className="flex items-center justify-between"><div><h2 className="font-semibold">Business pulse</h2><p className="mt-1 text-sm text-muted-foreground">Your activity will appear here.</p></div><button className="text-sm text-emerald-400">View report</button></div><div className="mt-10 grid h-40 place-items-center rounded-lg border border-dashed border-white/10 bg-white/[.02] text-center"><div><p className="font-medium text-slate-300">No activity yet</p><p className="mt-1 text-sm text-muted-foreground">Connect your business workflows in the next sprint.</p></div></div></article><article className="rounded-xl border border-emerald-400/15 bg-gradient-to-br from-emerald-500/15 to-transparent p-6"><Sparkles className="size-5 text-emerald-300" /><h2 className="mt-5 text-lg font-semibold">Your AI employee is getting ready</h2><p className="mt-2 text-sm leading-6 text-slate-300">Soon you’ll be able to ask clear questions and act with confidence from one workspace.</p><button className="mt-6 inline-flex items-center gap-2 text-sm font-medium text-emerald-300">Explore the roadmap <ArrowRight className="size-4" /></button></article></section></main>;
}
